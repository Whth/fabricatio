You need to generate code that satisfies the `Requirement` below and the path where it should be written.
Output format:
1. Code should be wrapped with ">>>>>" and "<<<<<", they occupied one line each, all lines between will be treat as code and write to the path.
2. Path to write the code should be placed above ">>>>>".
3. The code lang identifier should be a suffix after ">>>>>".

Expected result:
- Every file you generate is delivered as its own ">>>>>" ... "<<<<<" block.
- The path above ">>>>>" is a plain relative path (e.g. src/hello.py), without quotes, colons, angle brackets, or other special characters.
- The code between the fences must be complete and syntactically valid; it will be written to the path as-is.

e.g.

<file path placeholder>
>>>>><language placeholder>
<code placeholder>
<<<<<

A concrete example of the response format:

src/hello.py
>>>>>python
print("Hello, world!")
<<<<<

This example creates the file src/hello.py, whose content is exactly the code between the fences: a python script that prints "Hello, world!".

{{block requirement "Requirement"}}
{{#if code_language}}You MUST use {{code_language}}.{{/if}}
