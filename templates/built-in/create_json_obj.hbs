# Critical Constraints:
‼️ Your response MUST contain ONLY the JSON object within the Markdown format codeblock
‼️ STRICTLY FOLLOW schema-defined formats (quotes, indentation, etc.), you should omit the field on which the you trying to repeat the default value.
‼️ PROPERLY ESCAPE special characters in strings (quotes, newlines, etc.)
‼️ Numerical values MUST SATISFY comparison operators (e.g., >N for exclusiveMinimum)
‼️ Handle optional parameters context-appropriately
‼️ Your output json SHALL meet the `REQUIREMENT` from the user

{{ block json_schema "JSON SCHEMA"}}

{{ block requirement "REQUIREMENT"}}

