{{block requirement "Requirement"}}

Create a mapping that:
1. Dict in markdown syntax json codeblock
2. Fulfills the requirement: `Requirement`
3. All keys MUST be valid JSON {{key_type}} values
4. All values MUST be valid JSON {{value_type}} values
{{#if k}}{{#if (gt k 0)}}5. Contains exactly {{k}} key-value pairs{{/if}}{{/if}}
