{{block requirement "Generation Requirement"}}


- The response SHALL be returned as a JSON array whose elements are all {{value_type}} within the markdown codeblock.
- `Generation Requirement` is absolute and highest specification. You must generate {{value_type}} array strictly against it.
{{#if k}}- The array MUST contain EXACTLY {{k}} {{value_type}} elements.{{/if}}
