{{block requirement "Edit Requirement"}}
{{block content "Current Source"}}
Decide whether the `Current Source` satisfies the `Edit Requirement`.

Reply with ONLY a JSON boolean wrapped in a fenced code block tagged `json`,
nothing else. No prose, no explanation.

```json
true
```

If the requirement is satisfied, reply `true`. Otherwise reply `false`.
