Analyze the following chapter content and produce a structured summary for narrative continuity.

**Chapter:** {{chapter_title}}

{{#if previous_summary}}
**Starting State (from previous chapter — use as baseline to identify what changed):**
{{previous_summary}}

{{/if}}
{{block chapter_content "Chapter Content"}}

{{#if previous_summary}}
Describe what CHANGED from the starting state above — track transitions, not just the final state.
For `unresolved_threads`, move a thread to `resolved_threads` once it is concluded this chapter — never resurrect an entry already in `resolved_threads`.
For each character, update `character_knowledge` — MERGE new facts/events into their existing list. Never remove previously listed knowledge; only add what's newly revealed in this chapter.
For `numerical_stat`, carry forward every previously recorded entry, updating any value that changed this chapter to its NEW current value, and appending any new quantitative facts introduced here. Never silently drop a stat — if it persists unchanged, keep its prior value.
For `established_facts`, append any world-level canon newly set by this chapter. Never drop or silently rewrite an existing entry — if this chapter contradicts one, keep the original and add the corrected fact as a new entry noting the retcon.
{{else}}
Establish the initial state — this is the first chapter, so capture the baseline for all characters, threads, and emotional tone.
For `character_knowledge`, list every fact, event, name, relationship, or secret each character knows after this chapter.
For `numerical_stat`, record every important quantitative value established this chapter (counts, sizes, distances, elapsed time, attribute values, money, etc.) as `{desc, num}` pairs.
For `established_facts`, record every world-level fact the novel must honor from here on (kingdom names, magic rules, treaties, geography, succession, etc.).
{{/if}}
Always populate `last_scene` — a concise description of where and how this chapter ends, so the next chapter can open from the correct beat.
Respond entirely in "{{ language }}".