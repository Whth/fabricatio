# Character State Allocation

## Element
Title: {{ title }}
Description: {{ description }}

## Sub Elements
{{#each children}}
- {{ title }}: {{ description }}
{{/each}}

## Goal
Allocate the character's states below to the sub elements, in story order.

## Requirements
- Output a JSON array of arrays of CharacterCardDiff objects, one inner array per sub element in the order listed above.
- Assign every state to exactly one sub element; do not drop or duplicate states.
- Only include fields that actually change; leave unchanged fields out.
- Output `[]` for a sub element where the character does not change.
- Stay consistent with the arc; do not contradict earlier states.

Respond entirely in "{{ language }}".

## Character Arc
{{ chain }}
