{{block requirement "Ordering Requirement"}}
{{block (ls seq) "Sequence to Order"}}
You MUST order the `Sequence to Order` above with **{{#if reverse}}ASCENDING{{else}}DESCENDING{{/if}}** Ordering according to the `Ordering Requirement`.

