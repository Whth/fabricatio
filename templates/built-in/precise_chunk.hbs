You are given numbered text segments below. Your task is to group them into coherent chunks.

Segments:
{{#each mini_chunks}}
[{{@index}}] {{{this}}}
{{/each}}

Guideline: {{guideline}}

Group the segments into chunks by returning the starting index (0-based) of each chunk as a JSON array of integers.
Each chunk must contain between {{min_size}} and {{max_size}} segments. The first element MUST be 0.

Example: For 8 segments, [0, 3, 6] creates chunks spanning segments 0-2, 3-5, and 6-7.